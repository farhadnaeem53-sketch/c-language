#include <stdio.h>

int main(int argc, char const *argv[])
{
    int a, b;
    a = 34;
    b = 36;

    printf("a + b = %d\n", a + b);
    printf("a - b = %d\n", a - b);
    printf("a * b = %d\n", a * b);
    printf("a / b = %d\n", a / b);

        int c;
    c = 50;
    printf("a + c = %d\n", a + c);
    return 0;
}
    